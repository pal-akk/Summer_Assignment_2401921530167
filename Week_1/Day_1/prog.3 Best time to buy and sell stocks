class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int profit=0;
        int stk=prices[0];
        for(int i=0 ; i<prices.size() ; i++){
            if(prices[i]<stk) stk=prices[i];
            profit=max(profit,prices[i]-stk);
        }
        return profit;
    }
};
