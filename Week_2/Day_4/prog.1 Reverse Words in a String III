class Solution {
public:
    string reverseWords(string s) {
        int ind=0;
        for(int i=0 ; i<=s.size() ;i++){
            if(s[i]==' ' || i==s.size()){
                reverse(s.begin()+ind,s.begin()+i);
                ind=i+1;
            }
        }
        return s;
    }
};
